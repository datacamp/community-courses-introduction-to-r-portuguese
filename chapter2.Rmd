---
title_meta  : Capítulo 2
title       : Vetores
description : "Neste curso de R gratuito, nós iremos fazer uma viagem a Las Vegas, onde você irá analizar seus resultados nos jogos usando vetores em R! Depois de completar este capítulo, você será capaz de criar vetores em R, nomeá-los, selecionar elementos dele e compará-lo com diferentes vetores. Vamos nessa!"

--- type:NormalExercise xp:100 skills:1 key:d9b453dbdd
## Criando um vetor

Está com sorte hoje? É melhor que esteja, pois esse capítulo levará você para uma viagem a Cidade do Pecado, também conhecida como *Paraíso dos Estatísticos*!

Graças ao R e suas novas habilidades de análise de dados, você irá aprender como melhorar sua performance e alavancar sua carreira como profissional em jogos de azar. Este capítulo vai mostrar como você pode facilmente acompanhar seu progresso de apostas e como fazer algumas análises de suas ações passadas. Próxima parada: Vegas, baby... VEGAS!!

*** =instructions
- Você ainda se lembra do que aprendeu no primeiro capítulo? Então, declare a variável `vegas` e defina `"Go!"` como seu valor. Lembre-se de colocar as aspas e não se esqueça que R é case sensitive, ou seja, diferencia maiúsculas e minúsculas.

*** =hint
Simplesmente digite a seguinte linha no editor:
```
vegas <- "Go!"
```

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Declare a variavel vegas
vegas <-
```

*** =solution
```{r}
# Declare a variavel vegas
vegas <- "Go!"
```

*** =sct
```{r}
test_object("vegas", incorrect_msg = "Você declarou corretamente a variável `vegas`? Não se esqueça que R é case sensitive!")
success_msg("Ótimo! Vamos para o próximo exercício.")
```


--- type:NormalExercise xp:100 skills:1 key:fd427db76f
## Criando um vetor (2)


Certo, vamos nos focar!

Nessa sua trajetória da pobreza a riqueza, você irá fazer um extenso uso de vetores. Vetores são arrays (ou arranjo, em Português) de uma dimensão que podem armazenar dados numerics, character ou logical. Em outras palavras, vector é simplesmente uma ferramenta para armazenar dados. Por exemplo, você pode armazenar neles os seus ganhos e perdas diárias nos cassinos.

Em R, você pode criar um vetor utilizando a função Combine [`c()`](http://www.rdocumentation.org/packages/base/functions/c). Você deve separar os valores presentes em seu vetor utilizando vírgulas entre os parenteses. Por exemplo:

```
numeric_vector <- c(1, 2, 3)
character_vector <- c("a", "b", "c")
```

Uma vez criados esses vetores em R, você pode realizar cálculos neles.

*** =instructions
Complete o código para que `boolean_vector` contenha três elementos: `TRUE`, `FALSE` e `TRUE` (nesta ordem).

*** =hint
Armazene `c(TRUE, FALSE, TRUE)` na variável `boolean_vector` com o operador `<-`.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
numeric_vector <- c(1, 10, 49)
character_vector <- c("a", "b", "c")

# Complete o código para a variável boolean_vector
boolean_vector <-
```

*** =solution
```{r}
numeric_vector <- c(1, 10, 49)
character_vector <- c("a", "b", "c")

# Complete o código para a variável boolean_vector
boolean_vector <- c(TRUE, FALSE, TRUE)
```

*** =sct
```{r}
msg <- "Não altere o código que declara as variáveis `numeric_vector` e `character_vector`!"
test_object("numeric_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("character_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("boolean_vector",
            incorrect_msg = "Tenha certeza que você armazenou os valores corretos na variável `boolean_vector`. Use `c(TRUE, FALSE, TRUE)`. Não coloque aspas em torno de `TRUE` e `FALSE`! Outra coisa: não se esqueça de colocar os valores exatamente na ordem descrita nas instruções.")
success_msg("Perfeito! Perceba que adicionando espaços entre as vírgulas na função `c()` melhora a leitura do seu código. Vamos praticar mais criação de vetores no próximo exercício!")
```


--- type:NormalExercise xp:100 skills:1 key:9f41229dbc
## Criando um vetor (3)

Depois de uma semana em Las Vegas e nenhuma Ferrari na sua garagem, você decide que é hora de usar seus super poderes de análise de dados.

Antes de fazer sua primeira análise, primeiro você quer coletar todas as vitórias e perdas na última semana:

No vetor `poker_vector`:

- Você ganhou $140 na segunda-feira
- Você perdeu $50 na terça-feira
- Na quarta-feira você ganhou $20
- Já na quinta, você perdeu $120
- E por fim, na sexta-feira, você ganhou $240

No vetor `roulette_vector`:

- Você perdeu $24 na segunda-feira
- Você perdeu $50 na terça-feira
- Na quarta-feira você ganhou $100
- Já na quinta, você perdeu $350
- E por fim, na sexta-feira, você ganhou $10

Você jogou apenas Poker e roleta no cassino. Para ser capaz de usar estes dados em R, você quis criar as variáveis `poker_vector` e `roulette_vector`.

*** =instructions
Armazene os valores que você ganhou e perdeu na roleta na variável `roulette_vector`.

*** =hint
Para te ajudar nessa etapa, nós criamos o código que armazena os ganhos e perdas de Poker na variável `poker_vector`. Dê uma olhada no editor. Atribua o valor correto a variável `roulette_vector`, baseado nos números da descrição do exercício. Não se esqueça que as suas perdas são números negativos.

*** =sample_code
```{r}
# Vitorias e derrotas no Poker de segunda a sexta
poker_vector <- c(140, -50, 20, -120, 240)

# Vitorias e derrotas na Roleta de segunda a sexta
roulette_vector <-  
```

*** =solution
```{r}
# Vitorias e derrotas no Poker de segunda a sexta
poker_vector <- c(140, -50, 20, -120, 240)

# Vitorias e derrotas na Roleta de segunda a sexta
roulette_vector <- c(-24, -50, 100, -350, 10)
```

*** =sct
```{r}
test_object("poker_vector",
            incorrect_msg = "Você atribuiu o valor correto a variável `poker_vector`?")
test_object("roulette_vector",
            incorrect_msg = "Tenha certeza de atribuir o valor correto a variável `roulette_vector`. Não se esqueça de seguir a ordem correta!")
success_msg("Muito bem! Para ver o conteúdo dos vetores, tudo que você precisa fazer é digitar o nome da variável no console e apertar ENTER. Vamos para o próximo exercício?")
```


--- type:NormalExercise xp:100 skills:1 key:3b0b80b192
## Dando um nome ao vetor

Como um analista de dados, é importante ter uma visão clara dos dados que você está usando. Entender o que cada elemento significa é essencial.

No exercício anterior, nós criamos um vetor com suas vitórias e derrotas durante uma semana. Cada elemento do vetor se refere a um dia da semana, mas é difícil qual elemento pertence a um determinado dia. Seria legal se você pudesse mostrar qual valor se refere a um determinado dia.

Você pode dar um nome aos elementos de um vetor usando a função `names()`. Dá uma olhada nesse exemplo:

```
some_vector <- c("John Doe", "poker player")
names(some_vector) <- c("Name", "Profession")
```

Neste código, primeiro nós criamos um vetor chamado `some_vector` com dois valores (`John Doe` e `poker player`). Em seguida, nós atribuímos dois nomes para cada elemento do vetor. O primeiro elemento recebeu o nome `Name`, enquanto o segundo elemento recebeu o nome `Profession`. Se imprimirmos o vetor, ele ficará semelhante a uma tabela. Confira:

```
          Name     Profession
    "John Doe" "poker player"
```

*** =instructions
- O código a direita nomeia os elementos no vetor `poker_vector` com os dias da semana. Adicione um código para fazer o mesmo com o vetor `roulette_vector`.

*** =hint
Você pode usar `names(roulette_vector)` para definir os nomes da variável `roulette_vector`. Tenha certeza de usar os nomes dos dias da semana ao nomear o vetor. Não se esqueça que R é case sensitive!

*** =sample_code
```{r}
# Vitórias de Poker de Segunda-feira a Sexta-feira
poker_vector <- c(140, -50, 20, -120, 240)

# Vitórias de Roleta de Segunda-feira a Sexta-feira
roulette_vector <- c(-24, -50, 100, -350, 10)

# Nomeando os elementos do vetor poker_vector com dias da semana
names(poker_vector) <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

# Nomeando os elementos do vetor roulette_vector com dias da semana
```

*** =solution
```{r}
# Vitórias de Poker de Segunda-feira a Sexta-feira
poker_vector <- c(140, -50, 20, -120, 240)

# Vitórias de Roleta de Segunda-feira a Sexta-feira
roulette_vector <- c(-24, -50, 100, -350, 10)

# Nomeando os elementos do vetor poker_vector com dias da semana
names(poker_vector) <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

# Nomeando os elementos do vetor roulette_vector com dias da semana
names(roulette_vector) <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
```

*** =sct
```{r}
test_object("poker_vector",
            incorrect_msg = "Não altere os valores dentro da variável `poker_vector`; eles já foram inseridos para você.")
test_object("roulette_vector",
            incorrect_msg = "Não altere os valores dentro da variável `roulette_vector`; eles já foram inseridos para você.")
test_object("poker_vector",
            eq_condition = "equal",
            incorrect_msg = "Não altere o código que nomeia os elementos de `poker_vector`; foque em `roulette_vector`!")
test_object("roulette_vector",
            eq_condition = "equal",
            incorrect_msg = "Certifique-se que você definiu corretamente os nomes para o vetor `roulette_vector`. Utilize exatamente o mesmo código que utilizamos quando nomeamos a variável `poker_vector`.")
success_msg("Muito bem! Continue para o próximo exercício.")
```


--- type:NormalExercise xp:100 skills:1 key:6858c65a4a
## Dando um nome ao vetor (2)

Se você quer ser um bom estatístico, você precisa ser preguiçoso. (Se você já é preguiçoso, você tem um talento nato para ser um excepcional estatístico.)

No exercício anterior você provavelmente percebeu que é chato e frustrante digitar e redigitar informações como os dias da semana. Contudo, quando você olha de uma perspectiva mais alta, existem maneiras mais eficientes de fazer isso, como armazenar os dias da semana de um vetor em uma **variável**!

Assim como você fez com suas jogatinas de Poker e Roleta, você também pode criar uma variável que contenha os dias da semana. Desta forma você poderá usar e reutilizar seu código.

*** =instructions
- A variável `days_vector` (que contém os dias da semana) já foi criada para você.
- Use `days_vector` para definir os nomes de `poker_vector` e `roulette_vector`.

*** =hint
Você pode usar o código `names(poker_vector) <- days_vector` para definir os nomes dos elementos em `poker_vector`. Faça a mesma coisa em `roulette_vector`.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Vitórias de Poker de Segunda-feira a Sexta-feira
poker_vector <- c(140, -50, 20, -120, 240)

# Vitórias de Roleta de Segunda-feira a Sexta-feira
roulette_vector <- c(-24, -50, 100, -350, 10)

# A variavel days_vector
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

# Defina os nomes dos dias em roulette_vector e poker_vector
names(poker_vector) <-   
names(roulette_vector) <-
```

*** =solution
```{r}
# Vitórias de Poker de Segunda-feira a Sexta-feira
poker_vector <- c(140, -50, 20, -120, 240)

# Vitórias de Roleta de Segunda-feira a Sexta-feira
roulette_vector <- c(-24, -50, 100, -350, 10)

# A variavel days_vector
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

# Defina os nomes dos dias em roulette_vector e poker_vector
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector
```

*** =sct
```{r}
msg <- "Não altere os valores predefinidos nas variáveis `poker_vector`, `roulette_vector` ou `days_vector`."
test_object("poker_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)

test_object("poker_vector",
            eq_condition = "equal",
            incorrect_msg = "Você definiu `days_vector` em `poker_vector`?")
test_object("roulette_vector",
            eq_condition = "equal",
            incorrect_msg = "Certifique-se que você definiu `days_vector` como os nomes em `roulette_vector`.")
success_msg("Boa! Um conselho: evite duplicar códigos em qualquer situação. Continue para o próximo exercício e aprenda a fazer contas com vetores!")
```


--- type:NormalExercise xp:100 skills:1 key:da995f099f
## Calculando total de vitórias
Agora que você tem as vitórias de Poker e Roleta muito bem nomeadas nos vetores, você pode começar a fazer algumas mágicas com dados.

Você vai querer encontrar os seguintes tipos de informação:

- Quanto foi o seu total de perdas e ganhos por dia da semana?
- No total, você perdeu dinheiro no decorrer da semana?
- Você está ganhando/perdendo dinheiro na Roleta ou no Poker?

Para conseguir as respostas, você precisará fazer cálculos nos vetores.

É importante saber que se você somar dois vetores em R, ele faz as somas dos elementos por posição. Por exemplo, os três códigos abaixo são exatamente iguais:

```
c(1, 2, 3) + c(4, 5, 6)
c(1 + 4, 2 + 5, 3 + 6)
c(5, 7, 9)
```
Você também pode fazer cálculos usando as variáveis que representam os vetores:

```
a <- c(1, 2, 3)
b <- c(4, 5, 6)
c <- a + b
```

*** =instructions
- Faça a soma das variáveis `A_vector` e `B_vector` e armazene o total em `total_vector`.
- Inspecione o resultado exibindo o valor de `total_vector`.

*** =hint
Use o operador `+` para somar `A_vector` e `B_vector`. Use `<-` para armazenar o resultado em `total_vector`.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
A_vector <- c(1, 2, 3)
B_vector <- c(4, 5, 6)

# Faça a soma de A_vector e B_vector
total_vector <-

# Exiba total_vector

```

*** =solution
```{r}
A_vector <- c(1, 2, 3)
B_vector <- c(4, 5, 6)

# Faça a soma de A_vector e B_vector
total_vector <- A_vector + B_vector

# Exiba total_vector
total_vector
```

*** =sct
```{r}
msg <- "Não altere o conteúdo de `A_vector` ou `B_vector`!"
test_object("A_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("B_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("total_vector", incorrect_msg = "Certifique-se que `total_vector` contém a soma de `A_vector` e `B_vector`.")
test_output_contains("total_vector", incorrect_msg = "Não esqueça de exibir o valor de `total_vector`! Simplesmente escreva `total_vector` em uma nova linha.")
success_msg("Bom trabalho! Continue para o próximo exercício.")
```


--- type:NormalExercise xp:100 skills:1 key:2969d8ed65
## Calculando total de vitórias (2)

Agora que você entende como R faz operações aritméticas com vetores, é hora de termos algumas Ferraris na garagem! Primeiro, você precisa entender qual foi o total de lucros e perdas por dia na semana. O lucro total diário é a soma dos lucros e perdas que você teve no Poker por dia, e os lucros e perdas que você teve por dia na Roleta.

Em R, isso nada mais é que a soma de `roulette_vector` e `poker_vector`.

*** =instructions
Armazene na variável `total_daily` o total de quanto você ganhou e perdeu por dia (combinando Poker e Roleta).

*** =hint
Igual ao exercício anterior, apenas armazene a soma dos dois vetores na variável `total_daily`.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Vitórias no Poker e Roleta de segunda a sexta:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Armazene em total_daily o total de perdas e ganhos de cada dia
total_daily <-
```

*** =solution
```{r}
# Vitórias no Poker e Roleta de segunda a sexta:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Armazene em total_daily o total de perdas e ganhos de cada dia
total_daily <- poker_vector + roulette_vector
```

*** =sct
```{r}
msg = "Não altere nada na declaração das variáveis `poker_vector` e `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)

test_object("total_daily", incorrect_msg = "Tenha certeza que você armazenou a soma de `poker_vector` e `roulette_vector` na variável `total_daily`.")

success_msg("Excelente! Continue para o próximo exercício.")
```


--- type:NormalExercise xp:100 skills:1 key:e66a56b9f0
## Calculando total de vitórias (3)

Baseado na análise anterior, parece que você tem uma mistura de bons e maus dias. Isso não era o que seu ego esperava, e você se pergunta se teve alguma chance de você ter perdido mais dinheiro do que ganho durante a semana.

Uma função que pode responder a esta pergunta é [`sum()`](http://www.rdocumentation.org/packages/base/functions/sum). Ela calcula a soma de todos os elementos de um vetor. Por exemplo, para calcular o total de dinheiro que você perdeu ou ganhou no Poker, você pode fazer o seguinte:

```
total_poker <- sum(poker_vector)
```

*** =instructions
- Calcule o total de dinheiro que você perdeu e ganhou na Roleta e armazene seu valor na variável `total_roulette`.
- Agora que você tem os totais da Roleta e do Poker, você pode facilmente calcular `total_week` (que é a soma de todos os ganhos e perdas da semana).
- Exiba o valor de `total_week` .

*** =hint
Use a função [`sum()`](http://www.rdocumentation.org/packages/base/functions/sum) para ter o total de `roulette_vector`. `total_week` nada mais é do que a soma de `roulette_vector` e `poker_vector`.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Vitórias no Poker e Roleta de segunda a sexta:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Total de vitórias no Poker
total_poker <- sum(poker_vector)

# Total de vitórias na Roleta
total_roulette <-  

# Total geral de vitórias
total_week <-

# Exiba o valor de total_week

```

*** =solution
```{r}
# Vitórias no Poker e Roleta de segunda a sexta:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Total de vitórias no Poker
total_poker <- sum(poker_vector)

# Total de vitórias na Roleta
total_roulette <-  sum(roulette_vector)

# Total geral de vitórias
total_week <- total_roulette + total_poker

# Exiba o valor de total_week
total_week
```

*** =sct
```{r}
msg = "Não altere nada na declaração das variáveis `poker_vector` e `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("total_poker",
            incorrect_msg = "Tenha certeza de que você armazenou a soma de `poker_vector` em `total_poker`.")
test_object("total_roulette",
            incorrect_msg = "Tenha certeza de que você armazenou a soma de `roulette_vector` em `total_roulette`.")
test_object("total_week",
            incorrect_msg = "Tenha certeza de que você armazenou a soma de `total_roulette` e `total_poker` em `total_week`.")

test_output_contains("total_week", incorrect_msg = "Não esqueça de escrever `total_week` em uma nova linha para exibir o valor da variável.")
success_msg("Bom trabalho! Mas, sinto que tenho más notícias...")
```


--- type:NormalExercise xp:100 skills:1 key:f532f5332d
## Comparando total de vitórias

Ops, parece que você está perdendo dinheiro. É hora de repensar e adaptar sua estratégia. Isso requer uma análise mais profunda...

Após uma breve reflexão na jacuzzi do seu hotel, você percebeu que uma possível explicação poderia ser atribuida ao fato de que suas habilidades na Roleta não são tão boas quanto no Poker. Então, talvez os seus ganhos no Poker tenham sido maiores (ou `>`) que na Roleta.

*** =instructions
- Assim como no exercício anterior, calcule `total_poker` e `total_roulette`. Você precisará usar a função `sum()` duas vezes.
- Compare e verifique se o total de ganhos no Poker é maior que na Roleta. Simplesmente exiba os valores dessa comparação usando o operador `>`. O que você conclui: você deve focar no Poker ou na Roleta?

*** =hint
- Você já resolveu metade dessa questão no exercício anterior!
- Para saber se 6 é maior que 5, digite `6 > 5`. A mensagem de retorno terá um valor lógico (`TRUE` ou `FALSE`).

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Vitórias no Poker e Roleta de segunda a sexta:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Calcule o total de ganhos na Roleta e no Poker
total_poker <-
total_roulette <-

# Compare e verifique se você teve um total maior no Poker que na Roleta

```

*** =solution
```{r}
# Vitórias no Poker e Roleta de segunda a sexta:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Calcule o total de ganhos na Roleta e no Poker
total_poker <- sum(poker_vector)
total_roulette <- sum(roulette_vector)

# Compare e verifique se você teve um total maior no Poker que na Roleta
total_poker > total_roulette
```

*** =sct
```{r}
msg <- "Não altere nada na declaração das variáveis `poker_vector` e `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)

test_object("total_poker",
            incorrect_msg = "Tenha certeza de que você armazenou em `total_poker` a soma de `poker_vector`. Use `sum()`.")
test_object("total_roulette",
            incorrect_msg = "Tenha certeza de que você armazenou em `total_roulette` a soma de `roulette_vector`. Use `sum()`.")
test_output_contains("total_poker > total_roulette",
                     incorrect_msg = "Você comparou os totais? Para saber se `total_poker` é maior que `total_roulette`, simplesmente digite `total_poker > total_roulette`.")
success_msg("Bom trabalho! Continue para o próximo exercício.")
```


--- type:NormalExercise xp:100 skills:1 key:8d78be44e9
## Selecionando vetores: os bons tempos

Seu palpite pareceu estar certo. Parece que Poker é mais sua praia do que Roleta.

Outra possível rota para investigação é sua performance no começo da semana de trabalho comparada ao final da semana. Sabe, você tomou alguns coqueteis de Margarita no final da semana, então...

Para responder a essa questão, você só precisa focar na seleção de `total_vector`. Em outras palavras, nosso objetivo é selecionar elementos específicos do vetor. Para selecionar um elemento de um vetor (e mais tarde matrizes, data frames, etc...), você deve usar colchetes. Entre os colchetes você deve indicar quais elementos quer selecionar. Por exemplo, para selecionar o primeiro elemento do vetor, você deve digitar `poker_vector[1]`. Para selecionar o segundo elemento, basta digitar `poker_vector[2]`, e por aí vai. Note que o primeiro elemento do vetor tem index 1, não 0 como a grande maioria das linguagens de programação.

*** =instructions
Armazene os resutados de Poker da quarta-feira na variável `poker_wednesday`.

*** =hint
Quarta-feira é o terceiro elemento do vector `poker_vector`, o qual pode ser selecionado com `poker_vector[3]`.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Vitórias de Poker e Roleta de segunda-feira a sexta-feira:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Defina a nova variável baseada na seleção
poker_wednesday <-
```

*** =solution
```{r}
# Vitórias de Poker e Roleta de segunda-feira a sexta-feira:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Defina a nova variável baseada na seleção
poker_wednesday <- poker_vector[3]
```

*** =sct
```{r}
msg = "Não altere nada na declaração das variáveis `poker_vector` e `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_wednesday",
            undefined_msg = " Por favor, tenha certeza que definiu a variável `poker_wednesday`.",
            incorrect_msg = " Parece que `poker_wednesday` não está com o valor correto de `poker_vector`.")
success_msg("Excelente! R também permite a seleção de múltiplos elementos de um vetor de uma vez só. Aprenda como no próximo exercício")
```


--- type:NormalExercise xp:100 skills:1 key:1351521670
## Selecionando vetores: os bons tempos (2)

Que tal analisar os seus resultados do meio da semana?

Para selecionar múltiplos elementos de um vetor, você pode adicionar colchetes ao final do vetor. Você também pode indicar entre os colchetes quais elementos deve ser selecionados. Por exemplo: suponha que você quer selecionar o primeiro e o quinto dia da semana: use `c(1, 5)` entre os colchetes para fazer isso. Perceba que no código abaixo selecionamos o primeiro e quinto elemento de `poker_vector`:

```
poker_vector[c(1, 5)]
```

*** =instructions
Armazene os resultados do Poker de terça-feira, quarta-feira e quinta-feira na variável `poker_midweek`.

*** =hint
Use o vetor `c(2, 3, 4)` entre os colchetes para selecionar os elementos corretos de `poker_vector`.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Vitórias de Poker e Roleta de segunda-feira a sexta-feira:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Defina a variável baseado na seleção
poker_midweek <-
```

*** =solution
```{r}
# Vitórias de Poker e Roleta de segunda-feira a sexta-feira:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Defina a variável baseado na seleção
poker_midweek <- poker_vector[c(2, 3, 4)]
```

*** =sct
```{r}
msg = "Não altere nada na declaração das variáveis `poker_vector` e `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_midweek",
            incorrect_msg = "Parece que `poker_midweek` não contém os valores corretos de `poker_vector`. Você pode usar o vetor `c(2, 3, 4)` dentro dos colchetes.")
success_msg("Muito bem! Continue para o próximo exercício para se especializar em seleção de vetores e muito mais!");
```


--- type:NormalExercise xp:100 skills:1 key:27976b79f4
## Selecionando vetores: os bons tempos (3)

Eu sei, selecionar múltiplos elementos de `poker_vector` usando `c(2, 3, 4)` não é nada conveniente. Muitos estatísticos são pessoas preguiçosas por natureza, então eles criaram uma forma muito mais fácil de fazer isso: `c(2, 3, 4)` pode ser abreviado usando apenas `2:4`, o que irá gerar um vetor com todos os números naturais de 2 a 4.

Então, outra forma de achar os resultados do meio da semana é `poker_vector[2;4]`. Note que o vetor `2:4` é colocado entre os colchetes para selecionar os elementos de posição 2 até a posição 4.

*** =instructions
Armazene em `roulette_selection_vector` os resultados da Roleta de terça-feira a sexta-feira; faça uso de `:` se achar que é melhor para você.

*** =hint
Armazene a seleção de `roulette_vector` em `roulette_selection_vector` colocando `2:5` entre colchetes.

*** =sample_code
```{r}
# Vitórias de Poker e Roleta de segunda-feira a sexta-feira:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Defina a variável baseado na seleção
roulette_selection_vector <-
```

*** =solution
```{r}
# Vitórias de Poker e Roleta de segunda-feira a sexta-feira:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Defina a variável baseado na seleção
roulette_selection_vector <- roulette_vector[2:5]
```

*** =sct

```{r}
msg = "Não altere nada na declaração das variáveis `poker_vector` e `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_selection_vector",
            undefined_msg = "Tenha certeza de que definiu a variável `roulette_selection_vector`.",
            incorrect_msg = "Parece que `roulette_selection_vector` não contém a seleção correta de `roulette_vector`. Tenha certeza de usar os indíces corretos.")
success_msg("Incrível! O caracter de dois pontos é extremamente útil e frequentemente utilizado em programação em R, sendo assim, lembre-se dele. Prossiga para o próximo exercício.")
```


--- type:NormalExercise xp:100 skills:1 key:e6c263ddee
## Vector selection: the good times (4)

Another way to tackle the previous exercise is by using the names of the vector elements (Monday, Tuesday, ...) instead of their numeric positions. For example,

```
poker_vector["Monday"]
```

will select the first element of `poker_vector` since `"Monday"` is the name of that first element.

Just like you did in the previous exercise with numerics, you can also use the element names to select multiple elements, for example:

```
poker_vector[c("Monday","Tuesday")]
```

*** =instructions
- Select the first three elements in `poker_vector` by using their names: `"Monday"`, `"Tuesday"` and `"Wednesday"`. Assign the result of the selection to `poker_start`.
- Calculate the average of the values in `poker_start` with the [`mean()`](http://www.rdocumentation.org/packages/base/functions/mean) function. Simply print out the result so you can inspect it.

*** =hint
- You can use `c("Monday", "Tuesday", "Wednesday")` inside square brackets to subset `poker_vector` appropriately.
- You can use `mean(poker_start)` to get the mean of the elements in `poker_start`. You do not need the mean of all poker elements, but only of the first three days.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Select poker results for Monday, Tuesday and Wednesday
poker_start <-

# Calculate the average of the elements in poker_start

```

*** =solution
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Select poker results for Monday, Tuesday and Wednesday
poker_start <- poker_vector[c("Monday", "Tuesday", "Wednesday")]

# Calculate the average of the elements in poker_start
mean(poker_start)
```

*** =sct
```{r}
msg = "Do not change anything about the definition and naming of `poker_vector` and `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_start",
            incorrect_msg = "It looks like `poker_start` does not contain the first three values of `poker_vector`. You can use `c(\"Monday\", \"Tuesday\", \"Wednesday\")` inside square brackets to do this.")
test_output_contains("mean(poker_start)", incorrect_msg = "Have you correctly calculated the average of the values in `poker_start` and printed it out? Use `mean(poker_start)`.")
success_msg("Good job! Apart from subsetting vectors by index or by name, you can also subset vectors by comparison. The next exercises will show you how!");
```


--- type:NormalExercise xp:100 skills:1 key:f0f619c901
## Selection by comparison - Step 1

By making use of comparison operators, we can approach the previous question in a more proactive way.

The (logical) comparison operators known to R are:

- `<` for less than
- `>` for greater than
- `<=` for less than or equal to
- `>=` for greater than or equal to
- `==` for equal to each other
- `!=` not equal to each other

As seen in the previous chapter, stating `6 > 5` returns `TRUE`. The nice thing about R is that you can use these comparison operators also on vectors. For example:

```
> c(4, 5, 6) > 5
[1] FALSE FALSE TRUE
```

This command tests for every element of the vector if the condition stated by the comparison operator is `TRUE` or `FALSE`.

*** =instructions
- Check which elements in `poker_vector` are positive (i.e. > 0) and assign this to `selection_vector`.
- Print out `selection_vector` so you can inspect it. The printout tells you whether you won (`TRUE`) or lost (`FALSE`) any money for each day.

*** =hint
In order to check for which days your poker gains are positive, R should check for each element of `poker_vector` whether it is larger than zero. `some_vector > 0` is the way to tell R what you are after.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <-

# Print out selection_vector

```

*** =solution
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- poker_vector > 0

# Print out selection_vector
selection_vector
```

*** =sct
```{r}
msg <- "Do not change anything about the definition and naming of `poker_vector` and `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)

test_object("selection_vector", incorrect_msg = "It looks like `selection_vector` does not contain the correct result. Remember that R uses element wise operations for vectors.")
test_output_contains("selection_vector", incorrect_msg = "Don't forget to print out `selection_vector` by writing the variable name on a new line.")
success_msg("Great!")
```


--- type:NormalExercise xp:100 skills:1 key:2754fc5cd4
## Selection by comparison - Step 2

Working with comparisons will make your data analytical life easier. Instead of selecting a subset of days to investigate yourself (like before), you can simply ask R to return only those days where you realized a positive return for poker.

In the previous exercises you used `selection_vector <- poker_vector > 0` to find the days on which you had a positive poker return. Now, you would like to know not only the days on which you won, but also how much you won on those days.

You can select the desired elements, by putting `selection_vector` between the square brackets that follow `poker_vector`:

```
poker_vector[selection_vector]
```

R knows what to do when you pass a logical vector in square brackets: it will only select the elements that correspond to `TRUE` in `selection_vector`.

*** =instructions
Use `selection_vector` in square brackets to assign the amounts that you won on the profitable days to the variable `poker_winning_days`.

*** =hint
Use `poker_vector[selection_vector]` to select the desired elements from `poker_vector`, and assign the result to `poker_winning_days`.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- poker_vector > 0

# Select from poker_vector these days
poker_winning_days <-
```

*** =solution
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- poker_vector > 0

# Select from poker_vector these days
poker_winning_days <- poker_vector[selection_vector]
```

*** =sct
```{r}
msg = "Do not change anything about the definition and naming of `poker_vector` and `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("selection_vector", incorrect_msg = "Don't change the way `selection_vector` is calculated.")
test_object("poker_winning_days",
            incorrect_msg =  "It looks like `poker_winning_days` does not contain the correct result. Use `poker_vector[selection_vector]`.")
success_msg("Good job! Continue to the next exercise.")
```


--- type:NormalExercise xp:100 skills:1 key:59e8dcbbd5
## Advanced selection

Just like you did for poker, you also want to know those days where you realized a positive return for roulette.

*** =instructions
- Create the variable `selection_vector`, this time to see if you made profit with roulette for different days.
- Assign the amounts that you made on the days that you ended positively for roulette to the variable `roulette_winning_days`. This vector thus contains the positive winnings of `roulette_vector`.

*** =hint
Once you've correctly calculated `selection_vector`, you can again use `roulette_vector[selection_vector]` to select the positive results from `roulette_vector`.

*** =pre_exercise_code
```{r}
# no pec
```

*** =sample_code
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on roulette?
selection_vector <-

# Select from roulette_vector these days
roulette_winning_days <-
```

*** =solution
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on roulette?
selection_vector <- roulette_vector > 0

# Select from roulette_vector these days
roulette_winning_days <- roulette_vector[selection_vector]
```

*** =sct
```{r}
msg = "Do not change anything about the definition and naming of `poker_vector` and `roulette_vector`."
test_object("days_vector", undefined_msg = msg, incorrect_msg = msg)
test_object("poker_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("roulette_vector", eq_condition = "equal", undefined_msg = msg, incorrect_msg = msg)
test_object("selection_vector",
            incorrect_msg = "It looks like `selection_vector` does not contain the correct result. Use `roulette_vector > 0`.")
test_object("roulette_winning_days",
            incorrect_msg = "It looks like `roulette_winning_days` does not contain the correct result. Use `roulette_vector[selection_vector]`.")

success_msg("Great! This exercise concludes the chapter on vectors. The next chapter will introduce you to the two-dimensional version of vectors: matrices.")
```
